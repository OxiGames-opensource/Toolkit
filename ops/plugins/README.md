# Ops Plugins

Put custom Ops plugins here.

Each plugin must have:

- `config.json`
- `index.sh`
- `readme.md`

Required actions in `index.sh`:

- `iconAction`
- `init`

If required actions are missing, the plugin is shown as broken.